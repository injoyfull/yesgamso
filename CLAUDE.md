# CLAUDE.md — 예감소 웹사이트 작업 가이드

> 이 폴더에서 작업할 때 먼저 읽어주세요. 브랜드·디자인 시스템·지켜야 할 규칙이 담겨 있습니다.

---

## 프로젝트 정체

**예감소(예술감각연구소)** 의 공식 홈페이지.

> ### ⚠️ 예감소 vs 까불고 (2026-08-17 확정 — 헷갈리지 말 것)
> - **연구소 = 예감소(예술감각연구소).** 사이트 전체의 주체이자 브랜드. `예감소 : ON` 마크는 그대로.
>   `index.html` · `nado.html` · `en.html`은 **전부 예감소**. 손대지 말 것.
> - **프로젝트 진행 네이밍 = 까불고.** 워크숍/프로그램을 굴리는 팀 이름.
>   현재 적용 페이지는 `chul.html`(「철을 느끼다」 = **까불고 1기**) 한 곳뿐.
> - 이름 뜻 — 까(까불다 · 까 보다) / 불(불을 켜다 · 부르다) / 고(그리고 · GO · 잇는 어미).
>   태그라인: "'까불고'는 끝나는 말이 아니라 잇는 말이다 — 까불고, 느끼고, 부르고, 그리고 —"
> - **기수제**: 까불고 1기, 2기 … 숫자가 이어지는 것 자체가 컨셉.
> - **표기**: 대외 `까불고` 세 글자(붙여 쓰고 따옴표 없이). 공식 문서엔 `까불고 (예감소)` 병기.
> - **색**: `불`은 fire가 아니라 **Light** — 노란빛 `--bul`(라이트 `#E09400` / 다크 `#FFD24A`)로
>   `불` 한 글자에만. 주황 `--on`은 기존대로 CTA·강조. 마크업 `까<span class="bul">불</span>고`
> - 이름 정의서: `02_지원사업/실실실_진행자료/까불고_우리의_이름.html`

> ### /chul 회차 구성 (2026-08-17 기획안 최종 반영)
> | 회차 | 담당 | 제목 | 장소·시기 |
> |---|---|---|---|
> | 1차 | **행복수집가**(이한나) | 「감각 테스트의 날」 | 인조이풀 아트센터 · 10/17(토) 오후 2–4시 |
> | 2차 | **컬러수집가** | 「컬러를 수집하다」 | 당정섬 일대 야외 · 10/24(토) 오후 2–4시 |
> | 3차 | **인간수집가** | 「우리들의 문학의 밤」 | 지역 공간 야간 · 10/31(토) 저녁 |
> | 마무리 | — | 인스타그램 아카이빙 | 11월 (**오픈스튜디오 전시는 폐지** — 컨설팅 반영) |
>
> - 컨셉: **MBTI처럼 재미있는 감각 테스트** — "나는 어떤 감각의 사람일까?" 오감 5 + 히든 3.
> - ⚠️ **'검진'이라는 말은 쓰지 않는다** (2026-08-19 한나 결정). 검진은 "문제 있는 나를 확인받는"
>   무게가 있어 부담이 됨. 항상 **기대감의 언어**로: 검진→테스트, 검진센터→테스트룸,
>   문진표→감각 체크시트, 검진표→감각 기록지, 정밀검진 카드→레벨업 카드,
>   히든 감각 검진→히든 스테이지, 처방전→미션 카드. 의료 연상 소품(흰 가운)도 제외.
> - ⚠️ **'철(時/哲)' 개념은 참여자 대상 문구에서 설명하지 않는다** (2026-08-17 한나 결정).
>   「철을 느끼다」는 재단 공식 사업명이라 `<title>`·`<h1>`·푸터에만 남기고,
>   본문은 전부 '감각검진' 언어로 간다. 겉=유쾌한 검진 놀이 / 속=철. 개념은 우리가 들고 있는다.
> - 기준 문서: `실실실_진행자료/실실실_1차워크숍_기획안_최종.md`, `철을_느끼다_실행계획_개편안_v2.md`


- **핵심 문장**: **우리 모두는 원래 예술인이었다.**
  → *(구버전 "모든 인간은 예술인이다"는 선언조라 폐기. "원래 ~이었다"가 '잃어버린 걸 되찾는다'는 사이트 전체 논리와 맞물림)*
- **브랜드 표현**: `예감소 : ON` — 꺼진 감각을 켜는 스위치
- **팀(수집가)**: 이한나 **행복수집가**(인조이풀) / 김성겸 **인간수집가**(플레이업) / 박희진 **컬러수집가**(H.Hee·호아따따)
  → 각자 **1인 기업**으로 서서 함께 수익 구조를 만들어가는 협업 네트워크
- **연락**: `yesgamso@gmail.com`
- **거점**: 경기 하남

## 웹사이트의 목적 (우선순위 순)

1. **콜이 들어오게 하는 것** ← 1순위. 기업·기관·브랜드가 워크숍/협업을 의뢰하도록
2. 프로젝트가 계속 쌓이는 아카이브 (LAYERS)
3. 철학 전달

> 팀이 육아 중인 부모들이라 시간이 없음. 웹사이트가 영업을 대신해야 함.

---

## 사이트의 논리 (이 뼈대를 흔들지 말 것)

```
우리 모두는 원래 예술인이었다
  ↓
스위치를 끄고 싶어서 끈 사람은 없다      ← 죄책감 제거 = 위로의 출발
  ↓
잃어봐야 안다 (코막힘·화면 보며 먹은 저녁)
  ↓
사람마다 깨어 있는 감각이 다르다 → 내 감각의 지도를 알아차리는 일 = 나를 알아가는 시간
  ※ 칼 융에서 온 통찰이지만 이름은 쓰지 않는다 — 문장 자체로 승부 (트렌디·심플)
  ↓
살아 있다고 느끼는 건 감각이 살아 있을 때 → 예감소는 일상의 감각을 깨우는 일을 연구한다
  ↓
그래서 필요한 건 거창한 게 아니라 작은 경험
  ※ 까불고 1기 = 홈의 트리니티 **감각 → 행복 → 표현**을 세 번으로 편 것 (2026-09-04 대표 확정)
     1차 MY SENSE · 행복수집가 · 인조이풀 · "나, 이런 감각을 가진 사람이었구나" (내면, 의식 못 하던 감각 깨우기)
     2차 MY COLOR · 컬러수집가 · 당정뜰(야외) · 보이지 않던 색을 보게 됨, 좋아하는 색 → 내면의 색
     3차 MY STORY · 인간수집가 · 카페 대여(저녁) · **감각으로 쓰는 자기소개서** — 전시 + 나를 표현
     근거 문서: 02_지원사업/실실실_진행자료/1차워크숍_준비/실실실_1차워크숍_기획안_최종.html (v7)
  ※ 감각 = 오감 + 그 너머. 오감은 2천여 년 전 분류(이름은 쓰지 않음). 몸에는 고유감각(눈 감고 코끝 찾기)·
     내수용감각(심장박동 세기)·전정감각(균형)이 더 있고, "살아 있다는 느낌"은 이 안쪽 감각에서 온다 —
     이것이 "살아 있다고 느끼는 건 감각이 살아 있을 때"의 생리학적 근거. 육감/영감과는 선을 긋는다.
  ※ ⚠️ PSC(Pause·Sense·Create)는 **in:JOYFULL의 언어이므로 예감소 사이트에 쓰지 말 것.**
     한때 넣었다가 브랜드가 섞인다는 이유로 제거함. 되살리지 말 것
  ↓
우리도 살아내느라 수고했고, 겪어봤기에 안다 → 위로가 되고 싶어 모였다
  ↓
그래서 함께해요
```

---

## 파일 구조

| 파일 | 용도 |
|---|---|
| `index.html` | **홈**. 단일 파일 자체 완결형. `<head>`에 SVG 파비콘(스위치 마크) 내장 |
| `chul.html` | **「철을 느끼다」 초대장** (`/chul`) — 실·실·실 참여자 모집. 히어로 CTA와 LAYERS No.02가 연결 |
| `nado.html` | **「나도 예술가!」 아카이브** (`/nado`) — 2025 하남탐미 기록. LAYERS No.01이 연결 |
| `en.html` | **영문판** (`/en`) — 해외 문의용. 직역이 아니라 영어권 독자를 위해 다시 쓴 글 |
| `vercel.json` | `cleanUrls: true` — `.html` 없는 주소 |
| `CLAUDE.md` / `PLAN.md` / `LOG.md` | 작업 가이드 / 계획 / 결정 기록 — **모든 작업 시 함께 갱신할 것** |
| (스크래치패드) `yegamso_purple.html` | 아티팩트 미리보기용 사본. `<!doctype>/<html>/<head>/<body>` 없이 `<title>+<style>+본문+<script>`만 |

**중요**: 아티팩트 사본은 **직접 수정하지 말고 `index.html`에서 재생성**할 것 (title+style+body 추출).
스크래치패드는 세션이 바뀌면 사라지므로, 발행 직전에 매번 뽑아내는 게 안전함.

## 배포

| | |
|---|---|
| **GitHub** | `injoyfull/yesgamso` (공개), 브랜치 `master` |
| **Vercel** | `in-joyfull/yesgamso` — GitHub 연동됨 |
| **공개 주소** | **https://yesgamso.vercel.app** |
| 도메인 | 미구매. 추후 Vercel에서 `yesgamso` 구매 예정 (결제는 본인이 직접) |

```bash
git add -A && git commit && git push origin master   # 푸시하면 Vercel 자동 재배포
vercel deploy --prod --yes                            # 즉시 배포가 필요할 때
```

`.gitignore`가 `.env*`(Vercel OIDC 토큰)와 `.vercel`을 차단함 — 지울 것.

**아티팩트 미리보기**: https://claude.ai/code/artifact/db2fa349-65bc-4c59-9ee9-22521acd2510

---

## 디자인 시스템

### 컨셉: 「감각 표본실」
예감소 = 연구소. 일상의 사물(나뭇잎·스위치·눈·소리·실)을 **표본처럼 채집·분류**한다.
일상 속 **낯설음** — 표본들이 여백에 뜬금없이 나타나고, 스크롤하면 하나씩 켜진다.

### 컬러 — 로고에서 뽑은 오감 팔레트
**로고의 다섯 점 = 오감.** 이 발견이 브랜드와 철학을 포갬. 주인공은 **피치 + 블루** 둘.

| 토큰 | 라이트 | 다크 | 쓰임 |
|---|---|---|---|
| `--paper` | `#FCF8F0` | `#12100C` | 크림 바탕 |
| `--ink` | `#211D18` | `#F3EEE2` | 본문 |
| `--on` **피치** | `#EE6B4D` | `#FF836A` | 버튼·링크·ON·미각 |
| `--c-sight` **블루** | `#3B7CA8` | `#6FB4DC` | 진행 중·열린 초대·시각 |
| `--c-hear` 로즈 | `#E48BA0` | `#F0A5B8` | 청각 |
| `--c-smell` 오커 | `#E3A93F` | `#F0C060` | 후각·하이라이터 |
| `--c-touch` 세이지 | `#5F8A6A` | `#85B08E` | 촉각·표현 (최소 사용) |
| `--tree` | `#4A7355` | `#7FA98A` | 로고 나무 |

⚠️ **올리브/카키 그린 금지** — 크림 바탕에서 탁해 보여 반려됨. 그린은 세이지로, 쓰임도 최소.

### 타이포
- **산세리프만 사용**. 명조/세리프는 반려됨
- `--sans`: `'Apple SD Gothic Neo','Pretendard',...` / `--mono`: 라벨·번호·브랜드에만
- 제목 `font-weight:800`, `letter-spacing:-.025em`
- **자간을 넓히지 말 것** — 넓은 letter-spacing은 명시적으로 반려됨

### 섹션 라벨 — 영/한 2줄
```html
<span class="no"><span class="en">MANIFESTO</span><span class="ko">선언</span></span>
```
영문(초록, mono) 위 / 한글(회색) 아래. 전 섹션 공통 체계.
`MANIFESTO/선언 · FIVE SENSES/오감 · INVITE/초청하기 · LAYERS/켜 · ORIGIN/시작 · COLLECTORS/수집가 · CONTACT/연락`

**켜** = LAYERS의 우리말. 켜켜이 쌓인 층을 뜻하는 순우리말이자 '스위치를 켜다'와 같은 소리.

---

## ⚠️ 절대 지킬 규칙

### 1. 한글 조판 — 의미 단위로 끊는다 ★
```css
body{ word-break:keep-all; overflow-wrap:break-word; text-wrap:pretty; }
.nb{ white-space:nowrap }                    /* 붙임 처리 */
@media(max-width:700px){ .br-lg{ display:none } }   /* 넓은 화면에서만 개행 */
```
세 겹으로 관리한다:

1. **`word-break:keep-all`** — 어절 중간에서 절대 안 끊김 (기본)
2. **`.nb` 붙임 처리** — 한 덩어리로 읽혀야 할 구(句)를 감쌈.
   *손끝에 닿은 촉감 · 감각 창작 워크숍 · 다시 발견되는 기억* 같은 것들
3. **`text-wrap:balance`** — 큰 활자(`.pull .care .quote .shift .lead` 등)에 적용.
   마지막 줄에 한 단어만 떨어지는 **고아(orphan)**를 막고 줄 길이를 고르게 나눔

큰 활자는 여기에 더해 **`<br>`로 직접 끊는다.** 기준은 *문법이 아니라 의미*:

> 스위치를 끄고 싶어서 <br> 끈 사람은, 없어요.

"끈 사람은, 없어요"는 주어+서술어라 한 덩어리. 쉼표가 이미 쉼을 만드는데
거기서 또 끊으면 쉼이 두 번 된다. 모바일에서도 성립하는 개행만 순수 `<br>`로,
넓은 화면에서만 필요한 개행은 `<br class="br-lg">`로.

### 2. 문구 톤 — 부드러운 에세이체 (이어령 선생님 결)
- 어미: **~요 / ~지요 / ~어요**. "~입니다/~합니다" 단정조 지양
- 여린 부사: **살며시 · 가만히 · 어느새 · 차곡차곡 · 문득**
- **예외**: 버튼·"이런 곳에" 리스트는 또렷하게 (콜 받는 기능 유지)

### 3. 겸손이 아니라 당사자성 ★
**"우리는 평범한 아줌마 아저씨" 같은 자기 비하는 금지.**
→ 브랜드 담당자가 읽으면 단가가 내려가고 "아마추어인가?" 싶어짐.
대신 **"겪어봤기에 압니다"** — 살아낸 경험을 *전문성*으로 제시할 것.
같은 사실도 자기소개가 아니라 권위가 됨. 이게 다른 예술 강사와의 진짜 차별점.

### 4. 감각 ON/OFF 스위치 (핵심 인터랙션)
- 헤더 스위치를 끄면 `body.is-off` → **배경이 블랙으로 꺼짐**
- 단, **스위치 자체는 밝게 빛나야 함** — 깜깜한 방의 유일한 빛
- 다크 모드에선 히어로 헤드라인이 **네온처럼 켜짐**(text-shadow). OFF 시 함께 꺼짐

### 5. 입구(gate) — 방문자가 직접 켠다
처음 온 방문자는 **깜깜한 화면과 큰 스위치**를 만나고, 그걸 올려야 사이트가 열림.
자동으로 켜주던 예전 부팅 연출을 대체함 — 구경이 아니라 **행위**가 되도록.

- `<body>` 직후 인라인 스크립트가 첫 페인트 전에 `is-off` + `gated`를 입힘
- 켜는 순간: 노브 미끄러짐 → 0.42s 색 복귀 → 0.9s 게이트 페이드 → 1.7s DOM 제거
- **꺼진 상태는 반드시 JS로만 입힐 것** — HTML에 박으면 JS 실패 시 까만 화면에 갇힘
- **세션당 한 번만** (`sessionStorage: yegamso_on`) — 둘러보는 동안 재등장 금지
- 스위치뿐 아니라 **화면 아무 데나 클릭**, 키보드(Enter/Space/Esc)로도 열림
- `prefers-reduced-motion`이면 게이트 자체를 건너뜀
- 게이트는 홈(`/`, `/en`)에만. **`/chul`·`/nado`에는 넣지 말 것** — 공유 링크로 바로 들어오는 사람을 막으면 안 됨
- 게이트가 떠 있는 동안만 스크롤 잠금(모달이므로 예외). 열리면 즉시 해제

### 6. AI 티 나지 않게
반려된 것들: 명조 세리프, 넓은 자간, 칙칙한 크림색, 딱딱한 단정조.

---

## 현재 섹션 구성

1. **히어로** — 우리 모두는 원래 예술인이었다 + 나무에 열매가 열리는 부팅 연출
2. **MANIFESTO/선언** — 끄고 싶어서 끈 게 아니다 / 잃어봐야 안다 / 사람마다 다른 감각 / "그냥 좋았어요"→"식은 바람"
3. **FIVE SENSES/오감** — 다섯 점 = 다섯 감각. 감각→행복→표현 삼각
4. **INVITE/초청하기** — 워크숍 · 팝업·설치 · **콜라보레이션** + 열린 초대 배너
5. **LAYERS/켜** — No.03 「오감 스위치」 팀 키트(개발 중) → No.02 실·실·실 → No.01 하남탐미 *최신순*
6. **ORIGIN/시작** — 어쩌다 우리는 뭉쳤나 (살아내느라 수고했다 → 겪어봤기에 안다)
7. **COLLECTORS/수집가** — 세 사람 **캐릭터 일러스트** (⚠️ 모집·합류는 의도적으로 제거)
8. **CONTACT/연락** — 스위치 ON 그래픽 + 이메일

---

## 하지 말 것

- ❌ **PSC(Pause·Sense·Create) 쓰기** — in:JOYFULL의 언어. 브랜드가 섞여서 제거함
- ❌ **테스트·검진 등 평가·의료 어휘** — 1차 기획안 v7: "'검진'은 문제 있는 나를 전제한다. 기대의 언어로 간다." 참여자는 **평가받는 사람이 아니라 찾으러 가는 사람**. → **탐험 프레임**으로만: 웨이커 패스 · 탐사 구역 · 미지의 구역 · 탐험 일지 · 다음 원정 과제 · "아직 안 가본 구역"
- ❌ **학술 용어를 공개 페이지에** — 고유감각·내수용감각·전정감각 같은 말은 LOG/기획안까지만. 사이트에서는 "눈 감고 코끝을 찾는 감각"처럼 **풀어서** 쓴다 (기획안: "학술 용어는 참여자 앞에서 꺼내지 않는다")
- ❌ **"오감"을 감각의 전부인 것처럼 쓰기** — 대표: "우리가 생각하는 것이 오감만이 아니라서"(2026-08-31). 로고의 다섯 점은 **입구**이고, 그 너머(고유감각·내수용감각·전정감각 등 "학교에서 안 배운 감각")까지가 예감소의 범위. 단, **육감·영감과는 반드시 선을 긋는다** — 몸 안에 실재하는 감각만. 정체성 문장은 "인간의 **감각**을 회복시키는 예술감각연구소"
- ❌ **"감각검진" 대외용으로 쓰기** — 대표가 **내부용 단어로만 쓰기로 확정**함(2026-08-31). 새 네이밍 전달 예정. 공개 페이지(`index.html`·`chul.html`·`en.html`)엔 "감각 테스트"로 되돌려 둠. 새 네이밍이 오기 전까지 되살리지 말 것
- ❌ **연계(팝폴리오) 섹션 되살리기** — 아직 준비 중이라 뺐음
- ❌ **수집가 모집/합류 CTA 되살리기** — "아무나 다 오면 어떡하나" 우려로 뺐음
- ❌ **"시민" 대상 구분** — 관 냄새 남. 초청은 **대상(누구)이 아니라 형태(무엇)**로 나눔
- ❌ 자기 비하성 겸손 (위 3번)
- ❌ 세리프 폰트, 넓은 자간
- ❌ 외부 폰트/CDN 링크 (아티팩트 CSP 차단. 시스템 폰트만)
- ❌ 한쪽 파일만 수정

---

*마지막 업데이트: 2026-07-21*
